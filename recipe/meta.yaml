{% set name = "graphql-js" %}
{% set version = "15.3.0" %}
{% set sha256 = "a334a919ae3de651a3e7a88dbcb66a6b4f04f1eacd748221c5f6f25a42cfa7c5" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/graphql/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: generic
  script: npm install -g .

requirements:
  build:
    - nodejs

test:
  commands:
    - npm list -g graphql
  requires:
    - nodejs

about:
  home: https://github.com/graphql/graphql-js
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: A reference implementation of GraphQL for JavaScript
  description: |
    The JavaScript reference implementation for GraphQL, a query language for APIs created by Facebook. 
  doc_url: http://graphql.org/graphql-js/
  dev_url: https://github.com/graphql/graphql-js
